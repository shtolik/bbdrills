{
  "name": "bbdrills-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:unit": "vitest run"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "vitest": "^1.1.0"
  }
}
